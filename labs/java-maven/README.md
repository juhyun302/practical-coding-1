# Java Maven Lab

Maven 표준 디렉터리 구조에서 간단한 Java 애플리케이션과 JUnit 테스트를 구성한 실습입니다.

```bash
mvn test
mvn package
```

## Learned

- `pom.xml`을 이용한 의존성 및 빌드 설정
- `src/main/java`, `src/test/java` 역할 구분
- JUnit 기반 단위 테스트 실행
