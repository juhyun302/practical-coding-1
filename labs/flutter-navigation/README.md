# Flutter Navigation Lab

StatefulWidget과 Navigator를 이용해 두 화면 사이를 이동하는 기초 실습입니다.

## Learned

- StatefulWidget의 상태 변경
- GestureDetector 이벤트 처리
- `Navigator.push`와 route 구성
- 화면을 별도 파일로 분리하는 구조

Flutter 플랫폼 파일은 저장소 크기를 줄이기 위해 제외했습니다. 필요한 경우 이 폴더에서 `flutter create .`을 실행한 뒤 사용할 수 있습니다.
